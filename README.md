# JPA-Banking-System
create controller-entities-repository pakages
write application.properties to connection with database
create  webapp folder into the src/main/
in webapp floder we can store the HTML and JSP pages
